<h1>
  id=
  <%= @task.id %> の詳細ページ
</h1>

<p>タスクの内容:
  <%=@task.content %>
</p>

<p>タスクの状態:
  <%=@task.status %>
</p>

<%= link_to '一覧に戻る', tasks_path %>

<%= link_to 'この内容を編集する', edit_task_path(@task) %>

<%= link_to 'この内容を削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %>
